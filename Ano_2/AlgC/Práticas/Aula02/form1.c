#include <stdio.h>

int main (int argc, char* argv[])
{
    int n = atoi(argv[1]);
    int result = 0;
    int nAdd = 0;
    if(!(n==0 || n==1)) {
        
    }
    printf("Result = %d\n",r);
    printf("Sums = %d\n",nAdd);
    return r;
}